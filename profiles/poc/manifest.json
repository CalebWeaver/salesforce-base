{
  "profile": "poc",
  "displayName": "POC / Demo",
  "description": "Minimal structure for proofs of concept and demos. Uses TriggerHandler only — no SecurityEnforcer, no test data framework, no logging framework. Includes a promotion checklist for hardening into a real project.",
  "standalone": true,
  "templates": {
    "copy": [
      "templates/salesforce/classes/TriggerHandler.cls",
      "templates/salesforce/classes/TriggerHandler.cls-meta.xml"
    ]
  },
  "references": {
    "required": [],
    "optional": ["UniversalMock"]
  },
  "rules": {
    "standalone": "profiles/poc/rules-standalone.md"
  },
  "cursorRules": {
    "standalone": "profiles/poc/cursor-standalone.mdc"
  }
}
